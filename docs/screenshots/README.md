Drop screenshots here. Filenames referenced by README.md:

  hero.png        landing page or map view — the first image in the README
  map-view.png    /map: map with trip markers + trip cards below
  trip-form.png   the "Zaplanuj wyjazd" modal, partially filled in
  validation.png  a form showing validation errors
  theme.png       light vs dark theme
  ci.png          green GitHub Actions run
  tests.png       (optional) passing test output

Delete this file once the screenshots are in place.
